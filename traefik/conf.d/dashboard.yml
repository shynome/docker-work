http:
  routers:
    dashboard:
      service: api@internal
      rule: Host(`dashboard.traefik`)
      middlewares:
        - dashboard-auth

  middlewares:
    dashboard-auth:
      basicAuth:
        users:
          # docker run --rm -ti xmartlabs/htpasswd username password
          - 'username:$2y$05$cn7n.D2wuoJjXaoOZVdhJeNj5LY8tnJnGq/s6SnB9JKTBiokBUrti'
